import UIKit

// && is the AND operator
// || is the OR operator
// !  interis the NOT operator

var distance = 121
if distance > 5 && distance < 20 {
    println("AND")
}

var gameOver = true
while !gameOver {
    println("keep playing")
}